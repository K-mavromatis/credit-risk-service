# credit-risk-service: table of all changes

**Scope.** Repository `K-mavromatis/credit-risk-service`, branch `main` at `7be00bc`, from 2026-09-02 to 2026-09-19. The history has 8 regular commits and 6 pull-request merges. The five remote feature branches still present are fully merged into `main` (the branch for PR #1 is no longer on the remote), and no file has ever been deleted or renamed. Rows are in chronological order; the second column gives the commit and, where there was one, the pull request (from the merge-commit messages). Notebook rows come from cell-level diffs of `notebooks/01_data_preparation.ipynb`, not from raw JSON diffs.

**Change types.** Added, Refactored, Fixed, Config, Docs, CI / Tooling.

## 1. Change table

| Component / File | Type of Change | Description of Changes | Purpose / Problem Solved |
| --- | --- | --- | --- |
| `requirements.txt` | Added · `5da7ea1` | 26-line, unpinned dependency list in labelled groups: ML/data (pandas, numpy, scikit-learn, joblib), API (fastapi, uvicorn[standard], pydantic), database (sqlalchemy, alembic, psycopg2-binary), config (python-dotenv), frontend (streamlit), testing/quality (pytest, pytest-cov, ruff). | Declares the intended stack from day one so the environment can be recreated. It was never edited afterwards, so it has no version bounds and no `pre-commit`, both of which `pyproject.toml` later added. |
| `.env`, `.gitignore`, `README.md` (empty stubs) | Added · `5da7ea1` | Three zero-byte placeholder files created in the initial commit. | Reserve the config, ignore-rule and documentation entry points. `.env` is still tracked as an empty file, so no secrets were ever committed. |
| `.idea/` (`.gitignore`, `credit-risk-service.iml`, `misc.xml`, `modules.xml`, `material_theme_project_new.xml`, `inspectionProfiles/Project_Default.xml`, `inspectionProfiles/profiles_settings.xml`) | Config · `5da7ea1` | PyCharm project metadata (module definition, interpreter, inspection profiles, theme) committed with the first commit. | Byproduct of the initial `git add`. The later `.idea/` ignore rule does not un-track files that are already committed. |
| `README.md` | Added (Docs) · `b230c88` | 98 lines: badges, overview (Lending Club dataset, default-probability prediction), architecture table (Streamlit, FastAPI/Pydantic, scikit-learn/pandas, PostgreSQL/SQLAlchemy, Docker/Pytest/Git), tech stack, planned project tree, quick start (`pip install -r requirements.txt`, `DATABASE_URL`, `uvicorn api.main:app --reload`), five-sprint roadmap, team. | Documents the target architecture and onboarding path before any code exists. |
| `.gitignore` | Config · `b230c88` | 37 lines in six groups: secrets (`.env`, `.env.local`), virtualenvs, data/ML artifacts (`*.csv`, `*.tsv`, `*.xlsx`, `*.pkl`, `*.joblib`, `*.h5`), Python caches, pytest/coverage output, IDE/OS files. | Keeps secrets, raw data, model artifacts and caches out of version control. |
| `.idea/vcs.xml` | Config · `b230c88` | Adds the IDE's Git directory mapping. | Same as the `.idea/` row above; tracked even though `.idea/` is now ignored. |
| `notebooks/01_data_preparation.ipynb` | Added · `89f54e5` (PR #1) | New 13-cell notebook in six steps. (1) Imports and path config. (2) A 15-feature whitelist plus the target `loan_status` is loaded from `accepted_2007_to_2018q4.csv` with `usecols` (2,260,701 rows). (3) Rows are filtered to Fully Paid / Charged Off / Default and mapped to a binary `is_default` (1,345,350 rows kept). (4) Missing-value audit, `emp_length` parsed to 0-10, `term` stripped, `NONE`/`ANY` merged into `OTHER`. (5) `ColumnTransformer`: numeric median imputer plus `StandardScaler`; categorical most-frequent imputer plus `OneHotEncoder(handle_unknown="ignore")`. (6) Preprocessor fitted on all records, stress-tested on an adversarial applicant (NaNs, unseen categories), and the cleaned data written to `loans_filtered.csv` and `.parquet`. | Establishes the data-preparation pipeline and the 15-column feature contract that later code mirrors. This first version fitted the preprocessor on the whole dataset with no train/test split, which was corrected in `ec0fe1d`. |
| `pyproject.toml` | Added (Config) · `d0cab9c` (PR #2) | Package metadata (`credit-scoring-service` 0.1.0, Python >=3.10), runtime dependencies with lower bounds, a `dev` extra (pytest, pytest-cov, ruff, pre-commit), setuptools build backend, ruff settings (line length 88, `py310`, rules E/F/I, E501 ignored for notebooks) and pytest settings (`testpaths = ["tests"]`, `pythonpath = ["src"]`). | Single source of truth for packaging and tooling. Enables `pip install -e ".[dev]"` and the `src` layout. |
| `notebooks/01_data_preparation.ipynb` | Refactored · `d0cab9c` (PR #2) | Data paths moved from `ml_model/data/...` to `src/ml_model/data/raw/` and `src/ml_model/data/processed/`. Cells were reformatted in ruff-format style (trailing commas, collapsed multi-line calls). No logic change. | Aligns the notebook with the new `src` layout and the project's lint/format rules. |
| `README.md` | Refactored (Docs) · `d0cab9c` (PR #2) | Project-structure tree rewritten for the `src/` layout (`src/api`, `src/db`, `src/ml_model`, `notebooks/`, `pyproject.toml`, data folders). | Docs match the new layout. The quick-start section was left unchanged and still shows the old commands. |
| `.gitignore` | Config · `d0cab9c` (PR #2) | Adds `*.parquet`, `.ipynb_checkpoints/`, `src/ml_model/data/` and `.ruff_cache/`; removes three inline comments. | Keeps the new data directory, Parquet outputs and tool caches out of git. |
| `.pre-commit-config.yaml` | Added (CI / Tooling) · `147979d` (PR #3) | 7 lines: `ruff-pre-commit` at `v0.6.9` with the `ruff --fix` and `ruff-format` hooks. | Lints and formats automatically before every commit, so style is enforced rather than remembered. |
| `src/ml_model/service.py` | Added · `ec0fe1d` (PR #4) | New `CreditRiskPredictor` class. `__init__` loads a joblib pipeline (default `scoring_pipeline.pkl` next to the module) and checks that the file exists, deserializes and exposes `predict_proba`. `predict_proba(features: dict[str, Any]) -> float` wraps one record in a DataFrame, calls the pipeline, finds the class-1 column defensively and clamps the result to [0, 1]. Raises `FileNotFoundError`, `ValueError` (empty input) or `RuntimeError`. | Gives the API a single typed entry point for scoring, and turns sklearn/joblib failures into explicit errors. Its error message still names a former class, `CreditScoringService`. |
| `src/ml_model/generate_dummy_pipeline.py` | Added · `ec0fe1d` (PR #4) | Builds 200 synthetic rows shaped like the notebook features, trains `DummyClassifier(strategy="prior")` in a `Pipeline` (seed 42) and saves `scoring_pipeline.pkl` beside the module. | A throwaway stand-in that unblocks wrapper/API work while the real preprocessing pipeline is being fixed. Its docstring says not to use it for anything but plumbing and smoke tests. |
| `tests/test_ml_service.py` | Added · `ec0fe1d` (PR #4) | 29-line smoke script: builds a sample 15-field client record and prints the predicted default probability. Everything sits under `if __name__ == "__main__":` and there are no `test_*` functions. | Quick end-to-end check that the wrapper loads a pipeline and returns a probability. Verified: after running the generator it prints 0.4850 (the class prior), but `pytest` collects 0 tests from it. |
| `notebooks/01_data_preparation.ipynb` | Fixed · `ec0fe1d` (PR #4) | Step 6 now does a stratified 80/20 `train_test_split` (`random_state=42`) before fitting and fits the preprocessor on `X_train` only. The single `loans_filtered.csv`/`.parquet` export is replaced by `loans_train.parquet`, `loans_test.parquet` and `preprocessor.joblib`. Feature counts in the print statements are computed instead of hard-coded. | Removes the fit-on-full-data leakage of the first version: imputation medians, scaler statistics and one-hot categories are learned from training rows only. Persists the fitted preprocessor for reuse. The commit message mentions only the wrapper and smoke tests. |
| `src/credit_scoring_service.egg-info/` (`PKG-INFO`, `SOURCES.txt`, `dependency_links.txt`, `requires.txt`, `top_level.txt`) | Added (unintended build artifact) · `ec0fe1d` (PR #4) | Five files generated by `pip install -e .`. `top_level.txt` lists `api`, `db` and `ml_model`, although only `ml_model` exists. | Not a deliberate change: `.gitignore` has no `*.egg-info/` rule, so the install output was committed. |
| `.gitignore` | Config · `ec0fe1d` (PR #4) | Adds a "Local ML testing artifacts" group: `src/ml_model/generate_dummy_pipeline.py` and `src/ml_model/scoring_pipeline.pkl`. | Keeps the generated dummy model out of git. The generator was committed in the same commit, so the rule has no effect on it. |
| `.idea/credit-risk-service.iml` | Config · `ec0fe1d` (PR #4) | Marks `src` as a source folder. The file also records a local Windows interpreter path. | Lets the IDE resolve imports under the `src` layout. |
| `CONTRIBUTING.md` | Added (Docs) · `a265fab` (PR #5) | 174 lines in 11 sections: layer boundaries, setup, branch/commit conventions, code standards (typing, Pydantic v2, SQLAlchemy 2.0, Alembic, ruff), ML/data rules, testing (70% coverage target), PR checklist, review ownership, issue reporting, security. | Codifies standards and workflow for the two-person team. It refers to files not yet in the repo (`CHANGELOG.md`, `src/api/`, `src/db/`, `train.py`). |
| `notebooks/01_data_preparation.ipynb` | Fixed · `cd3784c` (PR #6) | Removes `int_rate` and `installment` from `FEATURE_WHITELIST` and `NUMERICAL_FEATURES`, so the model uses 13 features (9 numeric, 4 categorical) instead of 15. Also fixes a comment (`3/` to `3.`) and re-pastes cells with trailing whitespace (198 of 320 source lines now end in whitespace, up from none). | The commit message says it resolves target leakage. Inferred rationale: the interest rate and installment are set by the lender after its own risk assessment, so they act as proxies for the outcome. Stored cell outputs were not re-run and still show the 15-feature run. |

## 2. Commit and PR timeline

| Commit | Date | Author (as recorded in git) | PR / branch | Message |
| --- | --- | --- | --- | --- |
| [`5da7ea1`](https://github.com/K-mavromatis/credit-risk-service/commit/5da7ea1) | 2026-09-02 | Kostas | direct to `main` | init project structure and dependencies |
| [`b230c88`](https://github.com/K-mavromatis/credit-risk-service/commit/b230c88) | 2026-09-02 | Kostas | direct to `main` | docs: add project README and .gitignore |
| [`89f54e5`](https://github.com/K-mavromatis/credit-risk-service/commit/89f54e5), merge [`6d5b03a`](https://github.com/K-mavromatis/credit-risk-service/commit/6d5b03a) | 2026-09-07, merged 09-08 | Kostas; merged by Konstantinos Mavromatis | PR #1 `feature/data-prep-pipeline` | feat: complete data preparation and preprocessing pipeline |
| [`d0cab9c`](https://github.com/K-mavromatis/credit-risk-service/commit/d0cab9c), merge [`0d59c73`](https://github.com/K-mavromatis/credit-risk-service/commit/0d59c73) | 2026-09-10 | Kostas; merged by Konstantinos Mavromatis | PR #2 `refactor/project-structure-and-tooling` | refactor: reorganize project structure to src layout and configure ruff |
| [`147979d`](https://github.com/K-mavromatis/credit-risk-service/commit/147979d), merge [`8171f14`](https://github.com/K-mavromatis/credit-risk-service/commit/8171f14) | 2026-09-10 | Kostas; merged by liza-zinkina | PR #3 `ci/setup-pre-commit-hooks` | ci: add pre-commit configuration for ruff |
| [`ec0fe1d`](https://github.com/K-mavromatis/credit-risk-service/commit/ec0fe1d), merge [`0038a5e`](https://github.com/K-mavromatis/credit-risk-service/commit/0038a5e) | 2026-09-18, merged 09-19 | Kostas; merged by liza-zinkina | PR #4 `feature/scoring-service-wrapper` | feat: add CreditRiskPredictor wrapper and smoke tests |
| [`a265fab`](https://github.com/K-mavromatis/credit-risk-service/commit/a265fab), merge [`4114027`](https://github.com/K-mavromatis/credit-risk-service/commit/4114027) | 2026-09-19 | Liza0316; merged by Konstantinos Mavromatis | PR #5 `docs/contributing` | docs: add CONTRIBUTING.md guidelines |
| [`cd3784c`](https://github.com/K-mavromatis/credit-risk-service/commit/cd3784c), merge [`7be00bc`](https://github.com/K-mavromatis/credit-risk-service/commit/7be00bc) | 2026-09-19 | Liza0316; merged by Konstantinos Mavromatis | PR #6 `fix/data-preparation` | fix: resolve target leakage and optimize preprocessing |

## 3. Discrepancies found while tracing the history

| Observation | Evidence |
| --- | --- |
| The "smoke tests" are not pytest tests, so `pytest` collects 0 tests and coverage cannot be measured against the 70% target in `CONTRIBUTING.md`. | `tests/test_ml_service.py` has no `test_*` function; everything is under `if __name__ == "__main__":`. Running `pytest` reports "no tests collected". |
| The feature contract has drifted. The notebook now uses 13 features, but the dummy pipeline and the smoke record still use the old 15. | `NUMERICAL_FEATURES` in `generate_dummy_pipeline.py` and the sample record in `tests/test_ml_service.py` still include `int_rate` and `installment`. |
| Notebook outputs are stale, and they are committed although `CONTRIBUTING.md` says to clear outputs before committing. | Stored outputs still print "16 columns", "Numerical features count: 11", "Total model features: 15" and "(1000, 30)". |
| Files are tracked even though `.gitignore` lists them, and one generated folder is not ignored at all. | `git ls-files -ci --exclude-standard` lists `.env` (0 bytes), eight `.idea/` files and `generate_dummy_pipeline.py`. `src/credit_scoring_service.egg-info/` has no ignore rule. |
| A former class name survives in messages. | The error text in `service.py` and the docstring of `generate_dummy_pipeline.py` say `CreditScoringService`; the class is `CreditRiskPredictor`. |
| The README describes components that do not exist yet, and its quick start is out of date. | `api/`, `db/`, `frontend/`, `train.py`, `rf_model.pkl`, `test_api.py` and `test_ml.py` are not in `git ls-files`. The quick start still runs `uvicorn api.main:app` and installs the unpinned `requirements.txt`. |
| `CONTRIBUTING.md` requires a `CHANGELOG.md` entry per PR, but the repository has no `CHANGELOG.md`. | The project-docs copy of `CHANGELOG.md` stops at the pre-commit hook (PR #3) and is not in the git tree. |
